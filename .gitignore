# Secrets & runtime config — NEVER commit these
config/config.yaml
keys/*.pem
keys/*.key
keys/*.txt
*.pem

# Runtime artifacts
logs/
STOP
logs/paper_trades.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Session scratch (snapshot builds, temp HTML)
scratchpad/
